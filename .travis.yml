sudo: required 
language: generic 
services: 
  - docker 
before_install:
  - docker build -t eanene/react-app:travisversion -f Dockerfile.dev . 
script:
  - docker run -e CI=true eanene/react-app:travisversion npm run test